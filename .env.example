ADDRESS=localhost
PORT=8000
ENV=dev

# Disables the blockDirectAccess middleware
# Set to true when running without reverse proxy in front
ALLOW_DIRECT_ACCESS=false

# Disables rate limiting middleware
# Use with caution in production environments
DISABLE_RATE_LIMITER=false

# Logging
LOG_LEVEL=info               # debug/info/warn/error - console logging level only (file logging is always DEBUG)
LOG_RETENTION_WEEKS=4        # Number of weeks to keep log files (default: 4)
MAX_LOG_FILE_SIZE=104857600  # Maximum log file size before forced rotation (100MB, default: 100MB)

# Optional limits
MAX_REQUEST_BODY=1048576     # 1MB max request body
MAX_HEADER_SIZE=1048576      # 1MB max header size

# ── ANSM documents (RCP / patient notices) ─────────────────
# Kill switch: DOCS_ENABLED=false disables the feature entirely
# (/v1/medicaments/{cis}/rcp and /notice answer 501, no upstream
# fetching, no cache directory access)
DOCS_ENABLED=true
# Directory where cached RCP/notice documents are stored (created if missing).
# In production, keep it outside the deploy path (e.g. /var/lib/medicaments-api/docs)
DOCS_CACHE_DIR=./files/docs
# Politeness rate limit towards the ANSM upstream in requests per second
# (min 0.25, max 10) — separate from the API rate limiter
DOCS_FETCH_RATE_PER_SEC=2
# Timeout for a single upstream ANSM request
DOCS_FETCH_TIMEOUT=15s

# ── Build metadata ─────────────────────────────────────────
# APP_VERSION is automatically detected from git tags:
#   - On a tagged commit (release): Clean version (e.g., 1.2.0)
#   - Development build: Tag + commit info (e.g., 1.2.0-67-gb3c7af9-dirty)
# GIT_COMMIT & BUILD_DATE are automatically injected by Makefile at build time

# ── Observability ─────────────────────────────────────────────
# Local mode (default): config.alloy - obs stack via submodule
# Remote mode: config.remote.alloy - obs stack via tunnel
# Default is always local — remote requires explicit flag
ALLOY_CONFIG=config.alloy

# Remote endpoint URLs (only needed when ALLOY_CONFIG=config.remote.alloy)
# Cloudflare:  https://prometheus-obs.yourdomain.com/api/v1/write
# Tailscale:   http://100.x.x.x:9090/api/v1/write
# Local LAN:   http://192.168.1.x:9090/api/v1/write
# PROMETHEUS_URL=
# LOKI_URL=

# Cloudflare Access (only needed when using Cloudflare tunnel)
# CF_ACCESS_CLIENT_ID=
# CF_ACCESS_CLIENT_SECRET=

